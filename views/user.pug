extends layout

block layout-content
    div.View.UserView
        h1.Banner HEY, #{user.last_name} #{user.first_name}
        div.Message
            div.Title
                h3 U beat Barcelona again!
                h1 Great Game!
            //- pre.Details=JSON.stringify(userProfile, null, 2)
        div.NavButtons
            a(href="/logout")
                div.NavButton Log out